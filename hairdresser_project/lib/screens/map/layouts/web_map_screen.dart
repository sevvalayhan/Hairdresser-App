import 'package:flutter/material.dart';

class WebMapScreen extends StatelessWidget {
  const WebMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Web Map Screen"),
    );
  }
}
