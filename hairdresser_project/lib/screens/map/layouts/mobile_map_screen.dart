import 'package:flutter/material.dart';

class MobileMapScreen extends StatelessWidget {
  const MobileMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Mobile Map Screen"),
    );
  }
}
